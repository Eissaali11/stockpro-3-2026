import type { Express } from "express";

/**
 * Session Middleware Configuration
 * Temporary implementation - delegates to existing session setup
 */

export function sessionMiddleware(app: Express): void {
  // TODO: Implement modular session configuration
  // For now, delegate to existing session setup to maintain compatibility
  
  // This will be refactored to use the new infrastructure
  // Import and use existing session configuration temporarily
}